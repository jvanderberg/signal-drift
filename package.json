{
  "name": "lab-controller",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/electron/main.js",
  "scripts": {
    "dev": "tsx watch server/index.ts",
    "dev:client": "cd client && vite",
    "build": "tsc && cd client && vite build",
    "build:electron": "tsc -p tsconfig.electron.json",
    "start": "node dist/server/index.js",
    "typecheck": "tsc --noEmit && cd client && tsc --noEmit",
    "test": "npm run typecheck && vitest --run && cd client && npm run test:run",
    "test:run": "npm run typecheck && vitest run && cd client && npm run test:run",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "electron:dev": "concurrently -k \"npm run dev\" \"npm run dev:client\" \"wait-on tcp:3001 tcp:5173 && electron .\"",
    "electron:build": "npm run build && npm run build:electron && electron-builder",
    "dist:mac": "npm run build && npm run build:electron && electron-builder --mac",
    "dist:win": "npm run build && npm run build:electron && electron-builder --win",
    "dist:linux": "npm run build && npm run build:electron && electron-builder --linux",
    "postinstall": "electron-builder install-app-deps",
    "prepare": "husky"
  },
  "dependencies": {
    "better-sqlite3": "^12.5.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serialport": "^12.0.0",
    "usb": "^2.11.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "@types/ws": "^8.18.1",
    "concurrently": "^9.1.0",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "husky": "^9.1.7",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0",
    "vitest": "^2.1.0",
    "wait-on": "^8.0.1"
  }
}
